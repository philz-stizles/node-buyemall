
        {{#if shopCSS}}
            
        {{/if}}
        {{#if createProductCSS}}
            <link rel="stylesheet" href="/css/create-product.css">
       
    </head>
    <body>
        <header> 
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <div class="container"><a class="navbar-brand" href="#">Navbar</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item"><a class="{{#if activeShop}}active {{/if}}nav-link" aria-current="page" href="/">Shop</a></li>
                        <li class="nav-item"><a class="{{#if activeCreateProduct}}active {{/if}}nav-link" href="/admin">Create Product</a></li>
                    </ul>
                </div>
                </div>
            </nav>
        </header>

        {{{body}}}

        
    